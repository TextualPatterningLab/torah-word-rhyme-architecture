# Passage-inspection results

Each local inspection run creates a subdirectory under `results/passages/`.

The repository includes one frozen strict example, Deut. 11:10–15, because the Example Catalogue refers to that audit. Other user-generated outputs need not be committed.
